import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 0, 0, 0);
const secondaryColor = Color.fromARGB(255, 56, 56, 77);
const darkColor = Color.fromARGB(210, 0, 0, 0);
const bodyTextColor = Color(0xFF8B8B8D);
const bgColor = Color.fromARGB(255, 255, 255, 255);
const contrastColor = Color.fromARGB(255, 20, 255, 79);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 1); // we use it on our animation
const maxWidth = 1440.0;
